# bigtext (moved)

**This project has moved to [bookhockeys.com/bigtext](https://bookhockeys.com/bigtext/).**

Source now lives in the [`bigtext/` subfolder of the bookhockeys/book repo](https://github.com/itsavibecode/book/tree/main/bigtext) alongside the other utilities (`/greenline/`, `/shoovlator/`, `/hantavirus/`).

This repo now hosts a tiny redirect page so any existing bookmarks of `itsavibecode.github.io/bigtext` keep working.

## Why the move

To consolidate the small static utilities under one host. Same code, same features, new URL.

## Changelog

### v0.1.5 — 2026-06-12
- Replaced `index.html` with a meta-refresh + `<script>location.replace()` redirect to `https://bookhockeys.com/bigtext/`
- Kept the icon/manifest assets in place so cached references resolve cleanly
- All real changelog history continues in [the new home's README](https://github.com/itsavibecode/book/blob/main/bigtext/README.md)
